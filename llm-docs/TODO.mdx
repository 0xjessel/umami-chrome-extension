# TODO List

## High Priority

- [ ] The popup servername reflows on initial load. We should fix that to provide a smoother UI experience.
- [ ] Opening the popup has a long latency. We need to fix the performance and not have the network fetches block the UI updates.
